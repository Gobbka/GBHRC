#include "CanvasForm.h"

void Application::CanvasForm::update_markup(Application::Render::Engine* pEngine)
{
	//this->alloc_vbuffer(pEngine);
	//UINT size = 0;
	//for (auto* element : this->pElements)
	//{
	//	element->set_index(size);
	//	element->init(this);

	//	size += element->size();
	//}
}
